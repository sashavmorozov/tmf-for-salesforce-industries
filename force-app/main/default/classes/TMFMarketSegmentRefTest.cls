@isTest
private class TMFMarketSegmentRefTest {
    
    @isTest
    static void testTMFMarketSegmentRefConstructor() {
        TMFMarketSegmentRef marketSegmentRef = new TMFMarketSegmentRef();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}