@isTest
private class TMFProductTermTest {
    
    @isTest
    static void testTMFProductTermConstructor() {
        TMFProductTerm productTerm = new TMFProductTerm();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}
