@isTest
private class TMFResourceRefTest {
    
    @isTest
    static void testTMFResourceRefConstructor() {
        TMFResourceRef resourceRef = new TMFResourceRef();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}