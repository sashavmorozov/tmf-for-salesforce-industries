@isTest
private class TMFPriceAlterationTest {
    
    @isTest
    static void testTMFPriceAlterationConstructor() {
        TMFPriceAlteration priceAlteration = new TMFPriceAlteration();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}
