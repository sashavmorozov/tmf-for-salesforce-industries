@isTest
private class TMFPriceTest {
    
    @isTest
    static void testTMFPriceConstructor() {
        TMFPrice price = new TMFPrice();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}
