@isTest
private class TMFProductOfferingRefTest {
    
    @isTest
    static void testTMFProductOfferingRefConstructor() {
        TMFProductOfferingRef productOfferingRef = new TMFProductOfferingRef();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}
