@isTest
private class TMFProductPriceTest {
    
    @isTest
    static void testTMFProductPriceConstructor() {
        TMFProductPrice productPrice = new TMFProductPrice();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}