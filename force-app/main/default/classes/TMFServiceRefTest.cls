@isTest
private class TMFServiceRefTest {
    
    @isTest
    static void testTMFServiceRefConstructor() {
        TMFServiceRef serviceRef = new TMFServiceRef();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}
