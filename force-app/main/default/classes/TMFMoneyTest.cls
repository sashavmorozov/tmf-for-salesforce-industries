@isTest
private class TMFMoneyTest {
    
    @isTest
    static void testTMFMoneyConstructor() {
        TMFMoney money = new TMFMoney();
        Assert.isTrue(true, 'Test for an empty constructor, for future use');
        
    }
}
